import { CheckValueDirective } from './check-value.directive';

describe('CheckValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckValueDirective();
    expect(directive).toBeTruthy();
  });
});
