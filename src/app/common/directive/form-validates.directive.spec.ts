import { FormValidatesDirective } from './form-validates.directive';

describe('FormValidatesDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidatesDirective();
    expect(directive).toBeTruthy();
  });
});
